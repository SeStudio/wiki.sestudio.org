# foo

testing code blocks here

inline code `like this` shows up fine.

python:

```python
def hello(name):
    return "hi " + name  # whatever
```

bash:

```bash
mkdocs serve
```

js:

```js
let x = [1, 2, 3]
console.log(x)
```

yeah that works. go to [[bar]] or [[baz]]. theres a [[deep-dive]] page buried somewhere too.

![[blep.svg]]
