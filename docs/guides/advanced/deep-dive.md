# deep dive

this is two folders deep, just checking nested folders work.

??? example "code inside a dropdown"
    ```yaml
    site_name: SeStudio Wiki
    plugins:
      - search
      - roamlinks
    ```

back up to [[getting-started]] or [[foo]].
